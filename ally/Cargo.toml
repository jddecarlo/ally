[package]
name = "ally"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
ally_lib = { path = "../ally_lib" }
clap = { version = "3.1.17", features = ["cargo"] }
toml = "0.5.9"
